numpy==1.26.4
pandas==2.2.3
tqdm==4.65.0
joblib==1.5.1
scikit-learn==1.5.2
transformers==4.47.1
tokenizers==0.21.4
huggingface-hub==0.36.2
safetensors==0.5.3
esm @ git+https://github.com/evolutionaryscale/esm.git@v3.1.6
torch==2.4.1
biotite==0.41.2

# Keep the runtime stack compatible with the web server’s dedicated conda env
# rather than relying on whatever happens to be available in the host shell.
