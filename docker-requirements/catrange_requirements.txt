numpy==1.26.4
pandas==2.2.3
tqdm==4.65.0
joblib==1.5.1
scikit-learn==1.5.2
xgboost==2.1.4
# ChemBERTa substrate embeddings + XGBoost classifier only. ESM-C protein
# embeddings are computed in the shared `esmc` conda env (GPU offload / local
# fallback), so `esm` (and its `biotite` dep) are intentionally NOT installed
# here — that stack triggered "can't set attribute 'cls_token'" on load.
transformers==4.47.1
tokenizers==0.21.4
huggingface-hub==0.36.2
safetensors==0.5.3
torch==2.4.1
